Class PXW.DEV.BNF.Compilers.elementHandler
{

/// This will be matched with element.type.
Parameter Type;

/// set to POST to make this method called AFTER the element's children have been processed
parameter When;
}
